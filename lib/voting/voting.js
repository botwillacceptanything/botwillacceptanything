(function() {
    'use strict';

    var define = require('amdefine')(module);

    var deps = [
        'deferred'
    ];

    define(deps, function(deferred) {
        module.exports = {};
    });
}());